import React from 'react';

export const metadata = {
  title: 'Dependencies | Moti.do',
  description: 'View and manage task dependencies in your projects',
};

export default function DependenciesLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
} 